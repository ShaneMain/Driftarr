version: 2
updates:
  # Actions in .github/workflows are pinned to full commit SHAs (with a
  # `# vX.Y.Z` comment); Dependabot bumps both the SHA and the comment.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
