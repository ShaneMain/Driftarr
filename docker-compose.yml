# Root compose — includes all stacks
# Add or remove stacks as needed
include:
  - media/docker-compose.yml
  - downloads/docker-compose.yml
  - dashboard/docker-compose.yml
  - dns/docker-compose.yml
  # - monitoring/docker-compose.yml
  # - vaultwarden/docker-compose.yml  # password manager — enable off-site backups too
