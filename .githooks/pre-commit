#!/usr/bin/env bash
# pre-commit — block the commit if any staged file contains a literal .env value.
# Enabled by `git config core.hooksPath .githooks` (run setup.sh to set it).
exec "$(git rev-parse --show-toplevel)/scripts/secret-guard.sh" staged
